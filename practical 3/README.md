Summary:

Max Heap Sort is a sorting algorithm that uses a Max Heap, where the largest element is always at the root.

Steps:
Build a Max Heap from the array.
Swap the root (largest element) with the last element.
Consider that last element sorted.
Heapify the remaining elements to restore the Max Heap.
Repeat until the entire array is sorted.
